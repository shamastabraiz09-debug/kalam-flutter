name: kalam
description: Kalam - Two Way Voice Translator
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  speech_to_text: ^6.6.0
  flutter_tts: ^3.8.0
  google_fonts: ^5.1.0
  provider: ^6.1.2
  http: ^1.1.0
  intl: ^0.18.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true